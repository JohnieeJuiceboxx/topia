export default function listUsersInView(
  users,
  positionX,
  positionY,
  screenWidth,
  screenHeight
) {
  console.log("__listUsersInView___", users);
  const usersInView = [];

  // WRITE SOLUTION BELOW. ADD USERNAME AND IS_BROADCASTER TO 'usersInView' IF USER FALLS INTO VISIBLE RANGE

  //** array of obj */
  //** {username: "", distance: , icon: based on broadcaster or not} */

  // END SOLUTION SECTION

  return usersInView;
}
